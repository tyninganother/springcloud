server:
  port: 9001
spring:
  application:
    name: order-server

eureka:
  client:
    # 这个标记就是为了告诉eureka-server这个client要入住的
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://eureka.server.1.com:7001/eureka/,http://eureka.server.2.com:7002/eureka/
  instance:
    instance-id: order-server-9001
    prefer-ip-address: true